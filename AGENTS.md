# Agent notes — azuremastery.app

Applies to every agent working in this repo: Claude Code, Codex, Copilot, and
anyone driving them.

**Pushing `main` publishes immediately.** GitHub Pages serves this branch. Do not
merge or push to `main` without the owner asking for it.

---

## Never hand-edit an exam or question count

Every exam count, question total and cert-path figure on this site is written by
a tool. There are roughly 300 of them, repeated across `index.html`, 35 exam
pages, `llms.txt`, `llms-full.txt` and two social images — about ten per exam page
alone, across `<title>`, meta, OpenGraph, Twitter, JSON-LD, hero stats, body copy
and cross-referencing "related" cards that cite *other* exams' counts.

They were hand-maintained once. 19 of 27 pages were stale.

```bash
# after any change to the app's question banks or catalogue
python3 Tools/sync-marketing-counts.py --refresh    # re-read the app repo, patch, re-render
python3 Tools/sync-marketing-counts.py --check      # dry run, exits 1 on drift
```

It is idempotent. If a number looks wrong, fix the source and re-run — do not
edit the HTML.

## Where the numbers come from

The **app repo owns them**: `../AZ-104 Mastery` by default, override with
`AZURE_MASTERY_APP_REPO` or `--app-repo`.

| Figure | Source |
|---|---|
| Headline exam count, question total, prose label | `App/AppStoreAssets/catalog-totals.json`, read **verbatim** |
| Per-exam page count | `examQuestionCount` in `App/AzureMastery/Resources/exam-catalog.json` |
| Cert-path count | `CertPathDefinition(` minus `replacedByPathID:` in `CertPathRegistry.swift` |

`catalog-totals.json` is generated by the app's `Tools/generate-exam-catalog.py`
and gated in that repo's CI. This repo **does not recompute it** — that is the
whole point. The site once claimed 35 exams while the app already knew four had
retired, because each side derived its own totals.

A snapshot lands in `data/exam-counts.json` so this repo stays re-runnable
without the app checkout.

## The two rules that keep getting reverted

**1. Only currently bookable exams are advertised.** An exam becomes non-current
the moment it carries *any* `lifecycle.retirementDate` — not when that date
passes. The app sets its `examQuestionCount` to 0, and that zero is the test used
here. AZ-500 left the counts before its 31 August 2026 date, deliberately.

Non-current today: **AI-102, AI-900, AZ-204, AZ-500, DP-100**.

They keep everything else — their `/exams/<code>/` page, sitemap entry, llms.txt
bullet, and a per-page count that falls back to the full bank size so the page
still states what it holds. Those pages catch people searching for a dead exam
and route them to the successor. **Do not delete them.** What they must not do is
appear in a headline count or an exam-code list introduced by one.

**2. Counts are exam-scoped, not raw bank sizes.** The app's blueprint classifier
splits every bank into exam-scoped and supplemental questions, and advertises
only the former — so AZ-104 reads 481 against a 651-question bank. Aggregates and
per-page numbers must share that one basis or they stop summing.

## What the tool owns vs. what you edit by hand

**Tool-owned — never hand-edit:** aggregate totals everywhere; per-exam counts;
`roadmap-category__count` pillar chips; `llms.txt` `### Group (N)` headings and
`## Exams covered (N)`; the `Retired &amp; retiring (N)` disclosure summaries;
the prose exam-code enumerations in meta/JSON-LD; the SEO `h1`'s
"and N More Microsoft Certifications".

**Hand-edited:** the social-image exam pills (each is hand-coloured and grouped,
so the tool only warns on drift); the `exam-link--retired` class and grouping on
homepage links; `exams/index.html` hub cards; `sitemap.xml`.

`warn_retired_markup()` reports hand-edited markup that disagrees with the
catalogue and fails `--check`. It is what caught AZ-204 sitting in the active
Azure group a month after it retired, and AB-650 and AI-500 shipping with pages
but no hub card. The grouping is labelled "Retired & retiring" because a
non-current exam has not necessarily retired yet.

## Other tools

| Tool | Owns |
|---|---|
| `Tools/sync-marketing-counts.py` | every count on the site |
| `Tools/optimise-marketing-seo.py` | exam-page metadata, question-type previews, sitemap |
| `Tools/validate-marketing-seo.py` | the SEO contract; run after either of the above |
| `Tools/generate-llms-full.py` | `llms-full.txt`, derived from `llms.txt` — **not** run by the sync, so run it after |

`validate-marketing-seo.py` asserts its hand-kept `RETIRED` + `RETIRING` dicts
union to the app's non-current set, and exits with instructions when they drift.
Keep them in lockstep with `optimise-marketing-seo.py`'s `RETIRED_EXAMS` /
`RETIRING_EXAMS`. Do not add bare-number stale-phrase guards there — one for
`"full 319-question bank"` false-positived the moment AI-300 legitimately reached
319 exam-scoped questions.

## CI, and what it cannot see

`.github/workflows/counts.yml` runs `sync-marketing-counts.py --check --no-render`
and `validate-marketing-seo.py` on push, on PRs, and nightly.

**It only compares the committed `data/exam-counts.json` against the HTML.** The
app repo is private, so CI cannot clone it and cannot tell you the snapshot itself
has gone stale. That check happens only when someone runs `--refresh`.

So: after the app's catalogue changes, someone has to run `--refresh` here. The
app repo's `AGENTS.md` says the same thing from its side, and its
`Tools/check-marketing-claims.py` is what fires when the totals move.
